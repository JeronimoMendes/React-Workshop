import { Typography } from "@mui/material";
import React from "react";

// props.match.id to get book id
export const BookPage = (props) => {
	return (
		<div>
			<Typography variant="h1">Book page</Typography>
		</div>
	)
}